fun main() {
    try {
    var n = readln().toString()// write your code here, do not touch the lines above

    // do not touch the lines below    
    } catch(e: RuntimeException) {
        println("Well")
    } catch (e: Exception) {
        println("Wrong")
    }
}